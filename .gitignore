# Grabaciones / audio de juntas — no se versionan (archivos muy grandes).
# Se conservan en disco; en el repo viven sus transcripciones y resúmenes.
.obsidian/*.json
.obsidian/
docs/soporte/meetings/grabaciones/
*.wav
*.mp3
*.m4a
*.aac
*.flac
*.ogg

# Dependencias y builds de prototipos Node/Angular — se regeneran con
# `npm install` / `npm run build` (ver README de onboarding de cada prototipo).
node_modules/
dist/
.angular/
sona_pruebas_FILEY/

# Notas personales de handoff entre mis propias máquinas — no son para el equipo.
HANDOFF_OTRA_PC.md

# Entorno de Python del monolito. Se regenera con
# `py -3.13 -m venv .venv` + `pip install -r filey/requirements-dev.txt`.
.venv/
